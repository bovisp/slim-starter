{% extends 'layouts/app.twig' %}

{% block title %}Users{% endblock %}

{% block content %}

	<ul>
		{% for user in users %}
			<li>
				<a href="{{ path_for('users.show', { id: user.id }) }}">
					{{ user.fullName() }}
				</a>
			</li>
		{% endfor %}
	</ul>

{% endblock %}