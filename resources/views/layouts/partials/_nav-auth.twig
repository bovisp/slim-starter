<header class="navbar">
  <div class="container">
    <div class="navbar-brand">
      <a class="navbar-item" href="{{ path_for('home') }}">
        {{ getenv('APP_NAME') }}
      </a>
    </div>
  </div>
</header>